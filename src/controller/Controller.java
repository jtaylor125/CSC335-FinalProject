package controller;

import model.Card;
import model.CardStack;

public class Controller {
    private Card card; // for individual card discards based on user inputs
    private CardStack twoCribCards; // two cards that player sends to crib based on user inputs
}
